%!PS
<< /PageSize [595 842] >> setpagedevice

% paper size
/pagewidth 5 80 mul def
/pageheight 15 72 mul def

/box {w 0 rlineto 0 w rlineto -1 w mul 0 rlineto closepath} def

% ----- Define centimeter
/cm {28.346 mul} def
/w {10.8 cm} def
/ww {w 1.5 mul} def

/marginxoffset -305 def
/marginyoffset -350 def


/GujaratiMT-Bold findfont
%/Times-Roman findfont
%/ArialMT findfont
110 scalefont
setfont

/xindent {202} def
/yindent {700} def
/margin {25} def

/target (o) def

/oshow %stack: (string)
{ true charpath stroke } def

/GujaratiMT-Bold findfont 100 scalefont setfont

%0 -1 translate
%newpath 0 0 moveto (Joseph) oshow


/Helvetica-Bold findfont
145 scalefont setfont


/word {
    /dice  {rand 100 mod
            99
            lt}  def

    /size {y 0.13 mul} def

    /jitter {dice {0} {4} ifelse} def

    /y'   {y marginyoffset add} def
    /x'   {x marginxoffset add} def

    /y''  y' def%{y' jitter add} def
    /x''  x' def%{x' jitter add} def

    /yinv {10 y sub} def
    /g {0.0002 y mul} def
    /g' {0.9 g sub} def
    g' setgray

    /Helvetica-Bold findfont size scalefont setfont
    /target {dice {(JOSEPH)} {(JOSEPH)} ifelse} def
    /yz {y 0.1 mul } def

    0.12 0 translate
    0.2 rotate


    newpath
    x'' y'' moveto
    target
    oshow

}  def

%         startval inc    final     procedure
/wordline {w       ww     pagewidth  {/x exch def word}      for} def
/page     {w       3.8     pageheight { /y exch def wordline } for} def


0.8 setgray
-40.5 rotate
-20 0 translate
page

0.0 setgray
0 730 moveto
(JOSEPH) show


%0.0 0.0 0.0 setrgbcolor clippath fill

%showpage
%/x 72 def
%/y 738 def
%/fs 10 def
%/Helvetica fs selectfont
%(*) {x y moveto show /y y fs sub def y 36 lt {showpage /y 738 def}if} 256 string /Font resourceforall

showpage

% xindent yindent showText

% /yindent yindent margin sub def
% /Times-Roman findfont 15 scalefont setfont
% xindent yindent showText

% /yindent yindent margin sub def
% /Times-Roman findfont 10 scalefont setfont
% xindent yindent showText

% /yindent yindent margin sub def
% /Times-Roman findfont 9 scalefont setfont
% xindent yindent showText



% % ----- Define procedure for drawing a box
% /box {w 0 rlineto 0 w rlineto -1 w mul 0 rlineto closepath} def

% % take in account the size of a square
% /pagewidth pagewidth ww sub def
% /pageheight pageheight w sub def

% % draw all the boxes it can across a single row
% /rowboxes {w ww pagewidth {newpath y moveto box fill} for} def

% % increments the y variable and draws all the rows
% w ww pageheight { /y exch def rowboxes } for

%showpage